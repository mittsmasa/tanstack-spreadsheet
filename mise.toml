[tools]
node = "24.19.0"
pnpm = "11.20.0"

# 認証情報（BETTER_AUTH_SECRET / GOOGLE_CLIENT_*）は fnox が keychain から注入する。
# vite.config が server/auth.ts を読むため dev / build / preview のすべてで必要。
"github:jdx/fnox" = "1.32.0"
